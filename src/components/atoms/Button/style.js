import { StyleSheet } from 'react-native';
import constants from '../../../constants';

export default styles = StyleSheet.create({
    containerStyle: {
        flexDirection: 'row',
        alignItems: 'center',
        justifyContent: 'center',
        height: 40,//change height based on screen
        borderRadius: 20,//change border radius based on screen height
        backgroundColor: 'white'
    },
    textStyle: {
        color: 'white'
    },

});

